<?php
/** @var array $projects */
?>
<section>
	<h1 class="mb-4">Portfolio</h1>
	<div class="row g-4">
		<?php foreach ($projects as $p): ?>
		<div class="col-md-6 col-lg-4">
			<div class="card h-100 shadow-sm">
				<div class="card-body d-flex flex-column">
					<h2 class="h5 card-title mb-1"><?= htmlspecialchars($p['title']) ?></h2>
					<?php if (!empty($p['subtitle'])): ?>
					<h3 class="h6 card-subtitle text-secondary mb-2"><?= htmlspecialchars($p['subtitle']) ?></h3>
					<?php endif; ?>
					<p class="card-text flex-grow-1"><?= htmlspecialchars($p['lead'] ?? '') ?></p>
					<a class="btn btn-sm btn-outline-primary mt-2 align-self-start" href="/portfolio/<?= urlencode($p['slug']) ?>">Szczegóły</a>
				</div>
			</div>
		</div>
		<?php endforeach; ?>
	</div>
</section>
