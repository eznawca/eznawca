<?php
/** @var array $faq  lista [ ['q'=>..., 'a'=>...], ... ] */
?>
<section>
	<h1 class="mb-4">FAQ — najczęściej zadawane pytania</h1>

	<?php if (empty($faq)): ?>
	<p class="text-secondary">Pytania i odpowiedzi zostaną dodane w Fazie 3 (układ pod FAQPage / AEO).</p>
	<?php else: ?>
	<div class="accordion" id="faqAccordion">
		<?php foreach ($faq as $i => $item): ?>
		<div class="accordion-item">
			<h2 class="accordion-header">
				<button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#faq<?= $i ?>">
					<?= htmlspecialchars($item['q']) ?>
				</button>
			</h2>
			<div id="faq<?= $i ?>" class="accordion-collapse collapse" data-bs-parent="#faqAccordion">
				<div class="accordion-body"><?= htmlspecialchars($item['a']) ?></div>
			</div>
		</div>
		<?php endforeach; ?>
	</div>
	<?php endif; ?>
</section>
