<?php /** @var array $faq  lista pytań: q, a */ ?>
<section>
	<h1 class="mb-4">FAQ — najczęściej zadawane pytania</h1>
	<div class="accordion accordion-flush" id="faqAccordion">
		<?php foreach ($faq as $i => $item):
			$collapseId = 'faq-' . $i; ?>
		<div class="accordion-item">
			<h2 class="accordion-header">
				<button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#<?= h($collapseId) ?>">
					<?= $item['q'] ?>
				</button>
			</h2>
			<div id="<?= h($collapseId) ?>" class="accordion-collapse collapse" data-bs-parent="#faqAccordion">
				<div class="accordion-body"><?= $item['a'] ?></div>
			</div>
		</div>
		<?php endforeach; ?>
	</div>
</section>
